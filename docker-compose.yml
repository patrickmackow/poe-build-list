version: "3"

services:
  app:
    build: .
    ports:
      - 127.0.0.1:3000:3000
    restart: always
    depends_on:
      - mongo
  mongo:
    image: mongo
    volumes:
      - mongodata:/data/db
    restart: always

volumes:
  mongodata:

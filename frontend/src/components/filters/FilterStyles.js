import styled from "styled-components";

const Filter = styled.div`
  display: inline-block;
`;

const Label = styled.label`
  text-transform: uppercase;
  font-size: 0.9rem;
  color: #666;
  margin-right: 0.5em;
`;

const Select = styled.select``;

export { Filter, Label, Select };
